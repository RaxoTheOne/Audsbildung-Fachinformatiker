#include <stdio.h>

int main(){
    int kinder;
    int eier;
    int eierProKind;
    int rest;

    printf("Anzahl der Kinder: ");
    scanf("%d", &kinder);

    printf("Anzahl der Überraschungseier");
}